import { Pressable, SafeAreaView, StyleSheet, Text, View } from 'react-native';

export default function Page() {
    return (
        <SafeAreaView>
            <View>
                <Text>Projects</Text>
            </View>
        </SafeAreaView>

    );
}
